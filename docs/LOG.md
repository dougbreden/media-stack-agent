# Media Stack — Operations Log

Chronological record of setup decisions, fixes, and configuration changes.
For the full runbook see `RUNBOOK.md`. For day-to-day usage see `USAGE.md`.

---

| Date | Action | Notes |
|---|---|---|
| 2026-05-06 | Created folder structure | All 13 folders under config\ and data\ |
| 2026-05-06 | Created docker-compose.yml | 10 services including gluetun for Mullvad VPN |
| 2026-05-06 | docker compose up -d | All 10 containers running and stable |
| 2026-05-06 | VPN approach: Gluetun | Mullvad removed SOCKS5 — switched to Gluetun WireGuard container |
| 2026-05-06 | Gluetun connected | Singapore exit IP confirmed via Mullvad node |
| 2026-05-06 | qBittorrent configured | Password set, categories created, no proxy needed |
| 2026-05-06 | Prowlarr configured | FlareSolverr proxy (tagged), 4 indexers, Radarr + Sonarr apps connected |
| 2026-05-06 | Radarr configured | Hardlinks on, root /data/movies, download client via gluetun:8080 |
| 2026-05-06 | Sonarr configured | Hardlinks on, root /data/tv, download client via gluetun:8080 |
| 2026-05-06 | Unpackerr live | Real Radarr + Sonarr API keys set, connected to both |
| 2026-05-06 | Bazarr configured | Sonarr + Radarr connected, English profile, YIFY + Podnapisi + OpenSubtitles |
| 2026-05-06 | Jellyfin configured | Libraries /data/movies + /data/tv, NVENC hardware acceleration enabled |
| 2026-05-06 | Jellyseerr configured | Jellyfin + Radarr + Sonarr connected, quality profile: Any |
| 2026-05-06 | Existing movies imported | TinyMediaManager movies imported via Radarr, unmatched deleted for re-requesting |
| 2026-05-06 | TV shows skipped | Starting fresh — request via Jellyseerr going forward |
| 2026-05-06 | Stack fully operational | End-to-end pipeline working, Jellyfin Media Player installed |
| 2026-05-06 | Quality size limits fixed | Set max size to 0 (unlimited) in Radarr and Sonarr quality settings |
| 2026-05-06 | 1337x disabled | FlareSolverr cannot bypass Cloudflare on 1337x — re-test after future FlareSolverr updates |
| 2026-05-06 | Jellyseerr search automatically | Enabled on both Radarr and Sonarr connections so requests trigger immediate search |
| 2026-05-07 | qBittorrent save path fixed | Default path was `/downloads/` (doesn't exist); corrected to `/data/torrents/` in qBittorrent.conf. VPN and peers were fine — it was a disk write error causing "Errored" status |
| 2026-05-07 | VPN confirmed healthy | Public IP confirmed (Mullvad Netherlands). HTTP/HTTPS/DNS outbound all working through VPN tunnel |
| 2026-05-07 | Nyaa.si added | Added to Prowlarr for indexer-specific content types |
| 2026-05-07 | Indexers section added to runbook | Documents minimal set, recommended full set, and private tracker path |
| 2026-05-07 | Jellyfin auto-refresh wired up | Radarr and Sonarr both connected to Jellyfin via API key — library updates instantly on import, no manual scan needed |
| 2026-05-09 | Language custom formats added | Sonarr and Radarr: Non-English (-10000), Language:English (+300), Preferred Subs (+200, SubsPlease/Erai-raws/etc). French MULTi releases deleted and re-search triggered for preferred group replacements |
| 2026-05-09 | Gluetun DNS fixed (tracker blocking) | Switched from DoT to plain DNS: added DNS_UPSTREAM_RESOLVER_TYPE=plain and DNS_UPSTREAM_PLAIN_ADDRESSES=1.1.1.1:53. BLOCK_*=off alone was not sufficient — DoT upstream itself was filtering tracker domains |
| 2026-05-09 | Non-English format patched for MULTi | Added (?-i:\bMULTi\b) to Non-English regex (case-sensitive match for French scene convention). Previous regex missed KAF/MULTi packs that don't contain "FRENCH" in the title |
| 2026-05-09 | Dual Audio custom format added | Sonarr (id=5) and Radarr (id=3): +400 score. Matches Dual.Audio, DualAudio, Dual Multi. English dub now preferred over English sub when available |
| 2026-05-09 | qBittorrent seeding limits set | GlobalMaxRatio=1, GlobalMaxSeedingMinutes=30, ShareLimitAction=Remove. Ratio never triggers (no port forwarding = no upload); 30-min timer is the real cleanup mechanism |
| 2026-05-10 | TV series S01 complete | All episodes downloaded. Season pack search found preferred release group for missing episodes. Individual episode searches on public trackers returned 0 usable results — SeasonSearch command was required |
| 2026-05-18 | Non-English regex fixed | Replaced broken `(?-i:MULTi)` (inline case flag silently ignored by Sonarr) with `(?<!dual[\s.]?)\bmulti\b` (negative lookbehind). NanDesuKa MULTi E23 (9 audio languages) was grabbed incorrectly — removed, replaced with preferred release group. New regex confirmed: NanDesuKa MULTi scores -9700, HakataRamen Dual Multi unaffected |
| 2026-05-18 | Preferred Groups format added | Sonarr (id=6) and Radarr (id=4): +500 score. Groups: Yameii, HakataRamen, SubsPlease, Erai-raws, Judas, LostYears, Arg0. Creates a score gap so trusted groups always beat unknown groups and trigger automatic upgrades |
| 2026-05-19 | Native Jellyfin Server conflict fixed | Native jellyfin.exe (C:\Program Files\Jellyfin\Server) was running alongside Docker Jellyfin, both on port 8096. Remote/Tailscale connections were hitting the native server (wrong credentials). Stopped processes, removed JellyfinTray HKCU startup entry. Uninstall "Jellyfin Server" from Apps to permanently resolve — keep "Jellyfin Media Player" |
| 2026-05-20 | Gluetun iptables corruption fixed | All UDP torrent trackers failing with "Operation not permitted" after Watchtower update. DNS was fine (already plain). Fix: docker compose up -d --force-recreate gluetun && docker compose up -d qbittorrent. Added watchtower depends-on label to gluetun so future updates auto-restart qbittorrent |
| 2026-05-20 | Watchtower crash loop fixed | "client version 1.25 too old" crash loop after Docker Desktop update. Fix: added DOCKER_API_VERSION=1.44 to watchtower environment. Watchtower 1.7.1 running, scheduled 3 AM |
| 2026-05-20 | REMUX dead seeder removed | 49GB REMUX stuck at 19.8% with 0 seeds. Deleted from queue with blocklist=true. Radarr auto-grabbed YTS 1080p (24 seeds, 7 MB/s) |
| 2026-05-20 | TV series S1 complete | All S1 episodes downloaded after Gluetun UDP fix restored tracker connectivity. S2 actively airing |
| 2026-05-20 | Secrets moved to .env | WireGuard key, WireGuard address, Radarr API key, Sonarr API key moved from docker-compose.yml to .env. docker-compose.yml now uses ${VARIABLE} references. .gitignore updated |
| 2026-05-20 | Homarr dashboard added | New container on port 7575. Board pre-configured: 8 app tiles (Jellyfin, Jellyseerr, Radarr, Sonarr, Prowlarr, qBittorrent, Bazarr, Homarr) + 4 widgets (clock, Sonarr upcoming, Radarr upcoming, torrent status). API keys pre-loaded. Config at M:\Media\config\homarr\ |
| 2026-05-20 | Jellyfin HEVC encoding enabled | AllowHevcEncoding and EnableTonemapping enabled via API. Previously HEVC downloads forced a slow h264 transcode causing lag on iPhone |
| 2026-05-20 | Infuse identified as best iOS player | Direct-plays HEVC natively with no lag. Jellyfin iOS app also works at Max bitrate setting |
| 2026-05-20 | Jellyfin Smart subtitle mode enabled | SubtitleMode=Smart: auto-shows English subs for non-English audio, no subs for English audio. No more manual toggling |
| 2026-05-20 | Firewall script updated | Added Homarr port 7575 to setup-firewall.ps1 |
| 2026-05-21 | Private tracker seeding configured | Global ratio/seeding limits disabled. ShareLimitAction=Stop (pause, never delete). Per-category rules: movies/tv pause at ratio 1:1; movies-private/tv-private seed indefinitely |
| 2026-05-21 | Private categories created | movies-private and tv-private categories added to qBittorrent with save paths matching hardlink directories |
| 2026-05-21 | Private download clients added | Radarr and Sonarr: added "qBittorrent (Private)" client (category=movies-private/tv-private, removeCompleted=false, tag=private). Prowlarr: tag private indexers with `private` to auto-route via this client |
| 2026-05-21 | qBittorrent lockfile crash loop fixed | Stale /config/qBittorrent/lockfile (dated May 20 16:10) was preventing startup. Removed via docker exec, container healthy |
| 2026-05-22 | Infuse free cellular limitation confirmed | Infuse free allows library browsing on cellular but blocks video playback — Pro required for remote streaming. No error message surfaces this. |
| 2026-05-22 | Jellyfin iOS app adopted for cellular | Free alternative to Infuse Pro. Works on cellular via Tailscale. Max Streaming Bitrate must be set to Original in app settings or Jellyfin transcodes video unnecessarily. |
| 2026-05-22 | iOS HLS seek lag documented | Jellyfin iOS app (WebView) always remuxes to HLS — direct play not possible on iOS. Seeking restarts FFmpeg, causing 2–4 s blank screen. Expected behavior, not a bug. |
| 2026-05-22 | LAN Networks / Known Proxies left null | Docker bridge (172.18.0.1) makes all connections appear local to Jellyfin regardless of origin. These settings have no effect in this Docker setup and should remain unset. |
| 2026-05-22 | Remote bitrate limit removed | Set to 0 (unlimited) via Jellyfin API. At 2.1 Mbps HEVC the previous 10 Mbps limit was harmless but misleading. |
| 2026-05-22 | Tdarr added to stack | Container haveagitgat/tdarr on port 8265, GPU passthrough (same as Jellyfin). Libraries: movies (/data/movies) and tv (/data/tv). Flow "HEVC→H264" (id=N7tOvfd6i) created: checkVideoCodec → ffmpegCommandStart → ffmpegCommandSetVideoEncoder → ffmpegCommandCustomArguments → ffmpegCommandExecute → replaceOriginalFile. |
| 2026-05-23 | Tdarr flow encoder bug fixed | Root bug: Tdarr Node worker reads `k.inputsDB` not `k.inputs` from the flow config. Flow had only `inputs` fields, so the worker always read plugin defaults (outputCodec=hevc). Fixed by adding matching `inputsDB` fields to all flow plugins. |
| 2026-05-23 | Tdarr 10-bit HEVC support added | h264_nvenc cannot encode 10-bit (yuv420p10le/p010le). Fix: set hardwareDecoding=false (removes -hwaccel cuda so CPU handles 10-bit→8-bit), and added ffmpegCommandCustomArguments plugin with outputArguments="-pix_fmt yuv420p". Final ffmpeg command: h264_nvenc -qp 20 -preset p4 -pix_fmt yuv420p. |
| 2026-05-23 | 455 incorrectly encoded files reset | 17 files encoded with hevc_nvenc (pre-inputsDB fix) and 438 "Transcode error" files (10-bit failures) reset via bulk API. Both libraries scanFresh triggered. Queue re-populating at 542 fps GPU encode rate. |
| 2026-05-23 | Tdarr tile added to Homarr | Tdarr tile added to Homarr dashboard (port 8265). |
| 2026-05-23 | Jellyfin mobile user created | User created with 8 Mbps per-user bitrate cap. Allows cellular access without risk of saturating the uplink. |
| 2026-05-23 | iOS Mullvad + Tailscale conflict identified | iOS enforces one active VPN. Mullvad captures all traffic including 100.x.x.x, blocking Tailscale. Tailscale shows connected but all Tailscale IPs time out. Fix: disable Mullvad when using Tailscale on iOS. |
| 2026-05-23 | Jellyfin HLS broken for Tdarr-converted files | Jellyfin DB had stale HEVC codec info for files Tdarr converted to H.264. HLS requests applied hevc_mp4toannexb to H.264 streams → FFmpeg exited code 234. Direct play unaffected. Fix: library refresh. Diagnosed via `docker exec jellyfin ffmpeg ...` reproducing the exact failing command. |
| 2026-05-23 | Gluetun/qBittorrent startup hardened | Root cause of recurring tracker failures: Gluetun iptables state drifts bad after hibernate/Docker restart (not Watchtower — both containers excluded from auto-update). Fix applied in three places: (1) startup-stack.ps1 now force-recreates Gluetun on every boot and cleans qBittorrent lockfile before starting; (2) scripts/fix-vpn.ps1 created as one-shot manual recovery script; (3) nightly 2am scheduled task added via create-startup-task.ps1 to proactively reset before Watchtower's 3am run. |
| 2026-05-23 | update.ps1 overhauled | Comprehensive maintenance script: pulls images, applies updates, cleans lockfile, force-recreates Gluetun, waits for health checks (not fixed sleeps), verifies VPN tunnel via am.i.mullvad.net, checks qBittorrent for errored torrents, triggers Jellyfin library scan, prunes images, refreshes firewall. Failures accumulate and are reported in summary rather than aborting early. |
| 2026-05-23 | USAGE.md + LOG.md created | USAGE.md: simple end-user guide. LOG.md: status log extracted from RUNBOOK.md to keep the runbook focused on reference material. README.md renamed to RUNBOOK.md. |
| 2026-05-23 | Quality profile gap identified for SD-only content | Content with no HD source on Nyaa.si — only DVD 480p/576p releases exist. HD-1080p quality profile rejected all releases (DVD not in allowed list). Solution: use a profile that includes DVD in the allowed list with 1080p as the cutoff — Sonarr grabs DVD immediately and upgrades automatically if HD appears. |
| 2026-05-24 | tdarr-dedup-aac.ps1 run (Phase 1 AAC cleanup) | 39 duplicate AAC stereo tracks removed. Phase 1 temp flow (AddAACH264x) used hardcoded `-c:2 aac` with no deduplication check — added a second AAC track to 162 files where AAC was already the primary codec. Script queried Tdarr SQLite for those files, probed each via Jellyfin ffprobe, remuxed out duplicates. |
| 2026-05-24 | Tdarr Phase 2 deployed via tdarr-restore-hevc-flow.ps1 | New 13-node "Universal H264+AAC" flow using ffmpegCommandEnsureAudioStream replaces AddAACH264x temp flow. Two branches: H264 path copies video and ensures EN+JA AAC stereo (192k); non-H264 path GPU-encodes to h264_nvenc and ensures EN+JA AAC. Built-in deduplication prevents re-adding AAC to files that already have it. All 472 files reset, scanFresh triggered on both libraries. |
| 2026-05-24 | dedup-audio.ps1 created (general-purpose dedup) | Generalised from tdarr-dedup-aac.ps1. Catches any duplicate audio stream by signature (codec|channels|language), regardless of Tdarr history or codec type. Probe failures labelled "Probe skipped" (locked/in-use files — normal while Tdarr or qBittorrent is active) rather than "Failed" so they don't inflate the error count. |
| 2026-05-24 | library-report.ps1 created | Reports 4-library video codec distribution, standardisation %, missing AAC count, and duplicate stream count. Probes all media via Jellyfin ffprobe with size+mtime cache (library-report-cache.json, excluded from git). First run ~10 min; subsequent runs use cache. |
| 2026-05-24 | Library baseline: 61% standardised, 122 missing AAC | 472 files, 978.6 GB total. Movies (329 files, 855 GB): 78% H264, 22% HEVC, 64% standardised. TV (143 files, 123 GB): 84% H264, 8% AV1, 8% HEVC, 52% standardised. Tdarr queue: 96 non-H264 files remaining. |
| 2026-05-24 | dedup-audio.ps1 run: 12 structural duplicates cleaned | Pre-existing duplicates in Blu-ray rips and movie remuxes — duplicate AC3/EAC3 English dub tracks, duplicate surround AAC tracks from source mux. Various files had Phase 1 remnants or pre-existing structural duplicates. |
| 2026-05-24 | Radarr HD-1080p profile: Remux-1080p removed | REMUXes (20–50 GB lossless Blu-ray copies) are counterproductive in a Tdarr standardisation setup: Tdarr re-encodes them to h264_nvenc anyway, and lossless audio (TrueHD/DTS-HD) is unusable by iOS clients. Removed Remux-1080p (quality id=30) from the HD-1080p quality profile allowed list via PUT /api/v3/qualityprofile/4. Profile now: HDTV-1080p, WEB 1080p, Bluray-1080p. |
| 2026-05-24 | REMUX + SD content stalledDL diagnosed | Both stuck in qBittorrent, not Tdarr — neither file had been imported to the library. Movie REMUX: 25.7 GB Remux-1080p stalled at 93.6% with dead seeders. SD content (NanDesuKa DUAL) stalled at 94.3% alongside a fully-downloaded BD 576p batch. 25 BD batch episodes available for Sonarr manual import from /data/torrents/. Stalled SD download deleted by user. |
| 2026-05-24 | SD content quality issue root-caused | 480p release grabbed because the "Any" quality profile (cutoff=SDTV) allows DVD/480p quality, and the +400 Dual Audio score made it eligible. Resolved by deleting the stalled download. BD 576p batch is complete — use Sonarr → Wanted → Manual Import pointing at the batch folder to import remaining 25 episodes. |
| 2026-05-24 | Jellyfin cellular playback fixed: disabled AllowHevcEncoding | All iOS apps could browse library over Tailscale but video would not start (Swiftfin, Infuse timed out; Jellyfin app took 2 min). Root cause: `AllowHevcEncoding=true` caused Jellyfin to transcode H264 → HEVC via hevc_nvenc for every remote iOS client, competing with Tdarr for the GPU. First HLS segment took ~2 min to produce. Fix: set `AllowHevcEncoding=false` in encoding.xml, restart Jellyfin. Now Jellyfin remuxes the H264 stream directly into HLS segments without any GPU encode — first segment ready in under a second. Jellyfin iOS app confirmed working on cellular. |
| 2026-05-25 | qBittorrent active torrent limit raised 5 → 20 | With 30+ seeding torrents and only 5 active slots, each torrent was active <1/6 of the time — severely limiting upload contribution and private tracker ratio building. Raised max_active_torrents to 20 via API. |
| 2026-05-25 | Malicious torrent detected and removed | Downloaded torrent contained a .exe file. Sonarr flagged it as importPending warning but did not auto-blocklist. Manually removed from Sonarr queue with blocklist=true, deleted file (was not on disk), triggered episode re-search. Root gap: Sonarr treats executable-file warnings as warnings not errors, so autoRedownloadFailed does not fire. |
| 2026-05-25 | Mullvad account expired -- VPN down | All torrents stopped (no up/down speed). Gluetun cycled through multiple NL server IPs on ports 51820 and 51821, WireGuard handshake went out (tun0 TX>0) but received nothing back (RX=0). Confirmed account expiry by elimination; user topped up account. Switched SERVER_COUNTRIES from Netherlands to Sweden (same resolution, fresh servers). VPN reconnected immediately after account restored. |
| 2026-05-25 | check-downloads.ps1 created | Covers qBittorrent failure modes Radarr/Sonarr cannot see: (1) dead metaDL with 0 seeds older than 2h -- removed from qBittorrent and blocklisted in Radarr/Sonarr for auto re-search; (2) dangerous file extensions (.exe/.bat/.cmd etc.) in download directories -- deleted and blocklisted; (3) stalled 0% with 0 seeds older than 24h -- reported for human review. Integrated into check-stack.ps1 as step 4/5. |
| 2026-05-25 | Library standardization Phase 1 complete | dedup-audio fixed 15 duplicate streams. remux-library-to-mkv.ps1 converted 266/267 MP4s to MKV (1 corrupt file permanently unfixable due to data-stream corruption). Two bugs found and fixed in the remux script: (1) Test-Path/Remove-Item/Move-Item treating `[` `]` in filenames as wildcards -- fixed with -LiteralPath; (2) MP4 data streams (gpmd/tmcd/bin_data) incompatible with MKV -- fixed with -map -0:d to exclude data streams. Full Tdarr reset (444 files) + scanFresh triggered. Jellyfin library scan triggered. Tdarr now processing HEVC/missing-AAC queue. |
| 2026-05-25 | standardize-library.ps1 created | Ongoing automation: runs dedup-audio + remux-library-to-mkv + Tdarr scanFresh in the correct order. Wired into check-stack.ps1 as a weekly step (stamp file at .standardize-last-run). Covers the gap Tdarr cannot fill: MKV container standardization and duplicate stream cleanup for new downloads. |
| 2026-06-21 | library-report.ps1 cache-staleness bug fixed | library-report was counting stale cache entries for files deleted after Phase 1 MP4→MKV remux. True movie count is 366 (not 612), true TV count is 289 (not 291). Fix: added post-probe pruning step that removes all cache entries whose file paths no longer exist on disk. |
| 2026-06-21 | Phase 2 MKV remux + library junk cleanup | Remuxed 22/23 remaining non-MKV files (TV series HEVC MP4 batch + individual movies). One file fails permanently (data-stream corruption). Removed 203 RARBG/YIFY junk files from library. Fixed one TV episode double extension + Sonarr rescan. Fixed one movie's duplicate FLAC streams (3 streams removed). Both libraries now 100% MKV. |
| 2026-06-21 | Tdarr FileJSONDB empty — scanFresh re-triggered | Tdarr's file database was empty (0 files tracked). Triggered scanFresh on both libraries (IDs rUP5cniqB and nw7PJBmiV). Workers immediately started processing: 1 GPU transcode worker + 2 CPU health-check workers active. 20+ files queued. Remaining non-standard: 9 HEVC + 1 AV1 movies (20 missing AAC), 82 HEVC TV (37 missing AAC). Tdarr will resolve all via ongoing GPU transcoding. |
| 2026-06-22 | Maintenance scripts renamed and enhanced | check-stack.ps1 → maintain-stack.ps1 (adds disk-space check as step 1/7, file logging to logs/automation-YYYY-MM.log, 7-step flow). check-downloads.ps1 → maintain-downloads.ps1 (adds log summary). create-startup-task.ps1 → setup-scheduled-tasks.ps1 (adds MediaStack-Standardize nightly task at 03:30). All scripts write to shared log file using [System.IO.File]::AppendAllText (no BOM). |
| 2026-06-22 | Standardize-library: 4-step flow with Tdarr error reset | Added step 2/4: calls tdarr-reset-universal-files.ps1 (errored-only default) before remux, so Tdarr errors from prior runs are cleared automatically. Added -SkipTdarrReset parameter. Added file logging. Steps are now: dedup → Tdarr reset → remux → scanFresh. MediaStack-Standardize scheduled task registered (daily 03:30). |
| 2026-06-22 | check-missing.ps1: report monitored content with no file | New read-only script queries Radarr wanted/missing + Sonarr wanted/missing (with series lookup by ID). Groups TV by series with per-season episode counts. Optional -TriggerSearch flag fires SeasonSearch/MoviesSearch for all missing content. Current state: 12 missing movies (4 unreleased/theatrical), 742 missing TV episodes across 16 series. Some content not available on public trackers. |
| 2026-06-22 | Claude Code slash commands added | Added .claude/commands/ with 4 project-level slash commands: /check (library-report + compliance summary), /health (maintain-stack + fix summary), /missing (check-missing + cause analysis), /search (look up series/movie in Sonarr/Radarr + trigger search). |
| 2026-06-28 | Agentic substrate Phase 1+2 deployed | Branch feat/agentic-substrate pushed. Scripts: health-probe.ps1 (5-check observation, health.json, ntfy alerts), heal-invoke.ps1 (headless `claude -p` repair with 5min timeout and circuit breaker). Scheduled task MediaStack-HealthProbe registered (every 15 min, interactive user). Skills added: /status (health.json dashboard), /probe (live run), /heal (manual repair trigger), /audit (heal log + audit trail). Hook: PostToolUse on Bash/PowerShell → logs/claude-audit.log. |
| 2026-06-28 | Sonarr queue rot — 119 dead downloads cleared | **Root cause:** Sonarr accumulated 171 queue items: dead individual episodes from defunct release groups (zero seeds), stalled series packs at 0%, and non-English releases that bypassed the custom format filter. These clogged the queue, blocking real downloads. **Fix:** Bulk-removed 119 queue entries via Sonarr API (removeFromClient=true, blocklist=true). Paused non-English packs in qBittorrent. Triggered SeriesSearch for affected series. Result: new season pack grabs now downloading. **Remaining issues:** (1) Some files in /data/torrents/ root use non-standard naming (S01EP01 vs S01E01) — Sonarr cannot auto-parse; need manual import in UI. (2) Some older content not found on any public indexer. (3) Non-English custom format expanded to include additional languages (see CLAUDE.md). **Agentic gap:** health-probe.ps1 does not check Sonarr queue health — new check needed: alert if >10 queue items at 0% for >24h. |
| 2026-06-29 | Phase 2 verified end-to-end; 7 bugs fixed pre-merge | Failure-injection test (stop a non-critical container → probe → heal → recover) surfaced and fixed: (1) heal-invoke required an API key and skipped otherwise — `claude -p` actually works with ambient ~/.claude session auth, so the key is now optional; (2) heal set `$env:ANTHROPIC_API_KEY` to empty, which would clobber that ambient auth in the Start-Job child — now only set when non-empty; (3) probe called heal synchronously but the scheduled task has a 3-min limit while claude can take ~5 min (observed 4 min) — probe now launches heal **detached**; (4) `docker compose ps` without `-a` hid stopped containers, so a crashed container read as healthy — added `-a`; (5) detached launch passed a multi-word `-Description` via `Start-Process -ArgumentList @(...)`, which doesn't quote in PS 5.1, so heal died at param binding before logging — now a single quoted arg string; (6) `Get-AlertState` iterated `.PSObject.Properties` on an empty hashtable, persisting .NET member names (Keys/Values/Count/...) as bogus services in alert-state.json — type-guarded to PSCustomObject; (7) claude stdout mojibake in the heal log — set `[Console]::OutputEncoding=UTF8` in the job. Verified: degraded probe (exit 1) → detached heal → claude ran `docker compose up -d` → re-check clean → RECOVERED, alert-state regenerated clean. |
